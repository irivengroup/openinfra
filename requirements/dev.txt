# Development, test, CI, security and packaging tools only.
# These packages must not be required by the production runtime.
hatchling>=1.25
pytest>=8.0
pytest-cov>=5.0
ruff>=0.5
mypy>=1.10
bandit>=1.7
pip-audit>=2.7
build>=1.2
PyYAML>=6.0
