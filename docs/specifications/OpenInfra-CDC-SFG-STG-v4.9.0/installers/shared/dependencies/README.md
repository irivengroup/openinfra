# Dépendances installées automatiquement

Les installateurs doivent installer automatiquement les dépendances nécessaires au scope : runtime Python, runtime web si nécessaire, PostgreSQL client ou serveur, composants HA, certificats, firewall, observabilité, service manager et outils de validation.

L'installation échoue proprement si une dépendance critique ne peut pas être installée.
